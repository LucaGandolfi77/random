# ADR-0002 — Deterministic gap rules

Gap/dimension/gate logic lives in `backend/app/assurance_engine` as rule functions with documented
severity mapping. No LLM interpretation; missing data yields gaps. Accepted.
