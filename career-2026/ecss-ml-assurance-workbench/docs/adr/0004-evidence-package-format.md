# ADR-0004 — Evidence package format

ZIP with `evidence-package/` root, README, manifest.json (schema 3.0.0), checksums.sha256, per-topic
md/json/csv; DB manifest is the authoritative snapshot for verification. Accepted.
