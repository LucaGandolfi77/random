# Release notes — v0.7.0 (Continuous Assurance Prototype)

Added: CI result ingestion (JUnit/JSON), immutable snapshots, baseline comparison, deterministic
regression rules, Action Register, reproducibility records, Technical Review Pack, SBOM inventory,
Project Adapter SDK, validate-project CLI, provenance field checks. Changed: version metadata to
0.7.0. Security: no new secrets; path/zip protections maintained. Known limitations: frontend
Continuous Assurance/review-mode pages not yet wired (documented), scenario campaign runner is a
record-level scaffold (no simulator execution — honest "Not Executed" states), offline mode excluded
by design (read-only responsive UI). Status: Continuous Assurance Prototype; not certified.
