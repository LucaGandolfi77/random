# Software supply chain (v0.7.0)

Local inventory under security/sbom.json (CycloneDX-style). Status: dependency audit unavailable in
this session (no `npm audit` run — documented). Lockfile present (package-lock.json). No findings are
declared either way; absence of an audit is not an assurance statement. The inventory is a local
engineering artefact inside /career-2026.
