# Digital Twin / HIL test bench (Space AI V&V)

Deterministic lunar-descent bench with SimClock (sim/wall time, pause, dt, sync), physical plant,
safety monitor, telemetry collector and evidence export. Protocol v1.0 (in-process). MIL/SIL
executed; PIL requires adapter; HIL explicitly Not Executed (no hardware). Run: make test.
