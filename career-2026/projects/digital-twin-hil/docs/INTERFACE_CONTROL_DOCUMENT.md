# INTERFACE_CONTROL_DOCUMENT

See bench implementation in twinlab/bench.py (deterministic lunar-descent plant, SimClock, safety
monitor, evidence export). Fidelity level documented per component; local in-process IPC (protocol
v1.0). Updated by module author; synthetic data only.
