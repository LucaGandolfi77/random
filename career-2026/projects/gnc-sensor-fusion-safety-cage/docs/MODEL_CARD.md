# Model card (simulated ML)

Simulated ML altitude estimator (synthetic): truth + N(0,3 m) with injectable bias ramp, stale hold,
NaN and OOD flag. Intended use: demonstration of arbitration, not a trained model. Limits: synthetic;
ML confidence never a safety proof.
