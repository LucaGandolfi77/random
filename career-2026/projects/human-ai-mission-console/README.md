# Mission Operator AI Decision Console

Decision-support model where AI recommends and operator decides (accept/reject/defer/veto/request
evidence). Scenarios: lunar landing cage + telemetry anomaly (FDIR assistant). Authority matrix,
timeout safe-default, audit trail, workload metrics. Tests: 8 PASS. Run: make test.
