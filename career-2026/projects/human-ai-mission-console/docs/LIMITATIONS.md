# LIMITATIONS

Mission operator decision console (decision support only). AI authority limited to log/raise/mode-
review; operator-only actions (guidance change, abort, veto, mode switch) never automatic; safe
default on timeout. Audit trail and non-invasive workload metrics (no emotion inference). See
console/decision_console.py.
