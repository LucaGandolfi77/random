# Requirements method

Requirement kinds: SR/SUB/SWR/MLR with typed links; each requirement has version and status.
Change = status Changed + downstream Suspect (no automatic evidence updates). Review/action required.
