# Assurance case (summary)

Claim: IBP soundly bounds the model output over declared input boxes for ReLU networks. Argument:
method soundness (documented), tolerance guard, order-equivalence via logit. Evidence: evidence/*.json
with reproducibility hashes; counterexamples exported. Limits: synthetic, host-only, no SMT; not a
certification.
