# Incident response (lab procedure)

1) Detect via checksum/manifest/gate failures (audit trail). 2) Halt update path. 3) Revert to last
trusted pinned version or checkpoint. 4) Record evidence (hash, reason). 5) Review allowlist/trust
configuration. Local-only procedure.
