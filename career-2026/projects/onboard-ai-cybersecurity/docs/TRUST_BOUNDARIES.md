# Trust boundaries

- Trusted: repository storage root, pinned allowlist, checked-in fixtures.
- Untrusted: any uploaded/updated artefact until manifest+checksum verified; telemetry until range+age
  checks; archives with traversal names (never extracted).
