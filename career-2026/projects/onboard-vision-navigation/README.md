# Embedded Vision-Based Navigation demo

Safe-landing-zone classification on synthetic frames: pipeline (validation, features, logistic,
softmax, OOD proxy, temporal median, fallback), FP32 vs INT8, robustness slices, host latency
measurements. Not an autonomous guidance system. Tests: 7 PASS. Run: make test.
