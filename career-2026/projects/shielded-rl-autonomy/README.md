# Shielded RL for Safe Autonomy (simulated lunar descent)

Environment + deterministic runtime shield + policy comparison (deterministic, unshielded RL,
shielded RL, action-constrained RL, fallback). No real vehicle linkage; shield is rule-based, never
a second RL agent. Tests: 8 PASS. Run: make test / make compare.
