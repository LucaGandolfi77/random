# Mission-Grade Model Registry and Safe Deployment Lab (local)

Model registry (semver, hash, dataset/preprocessing/config baseline, evidence), explicit approval and
stage promotion (Development..Active, no auto-promotion), simulated shadow/canary, rollback with
allowlist + integrity + dry-run, audit trail. Tests: 9 PASS. Run: make test. No cloud/public
deployment; synthetic.
