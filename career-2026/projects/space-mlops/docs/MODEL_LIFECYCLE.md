# MODEL_LIFECYCLE

See mlops/registry.py (stages, approval, promotion rules, shadow/canary simulated, rollback gate with
allowlist + checksum + dry-run, audit trail). Synthetic local lab; no public deployment. Updated by
module author.
