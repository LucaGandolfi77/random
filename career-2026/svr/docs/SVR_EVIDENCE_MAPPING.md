# SVR evidence mapping

Each evidence row carries an SVR destination (currently 'SVR Section TBD' until authoritative SVR structure is provided). Mapping approach: category -> SVR section per ECSS-informed templates; pending authoritative references.
