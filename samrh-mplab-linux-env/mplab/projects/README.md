# Project Storage Convention

Store committed MPLAB X projects here, for example:

1. `mplab/projects/samrh707_unit_test.X/`
2. `mplab/projects/samrh71_unit_test.X/`

Keep generated code and user code separate inside each project by referencing source files from the shared repository tree instead of copying them into the project directory.